1、直接篡改id，无任何校验

2、存在sign签名或者token校验（非身份认证token）
需要解决sign和token验证机制，一般需要去逆向分析，这里以小程序为例
该小程序存在token认证，经过确认非账号token
![](pic/Pasted%20image%2020260608231155.png)
和明显，uid为用户id，通过校验token是否合法，合法根据id获取值
直接梭哈分析token生成机制


3、存在token校验机制和id（即多个校验机制），但后端在处理token的时候，先验证token，token认证通过认为一登陆，后续获取信息不直接从token获取，而是由从id获取，篡改id可越权

4、具备加密手段，需要逆向破解加密技术

5、jwt-token认证缺陷
	1、未签名
	2、对称签名，密钥可获得
	3、密钥破解
	4、未进行验签操作