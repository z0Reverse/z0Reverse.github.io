---
title: 安全实现
date: 2026-09-20 02:16:38
categories:
  - AndroidReverse
  - 加解密算法
tags:
  - 加解密
  - 算法逆向
---

# KeyStore
Android Keystore 是一个专用于安全存储加密密钥的系统。它允许应用在不直接暴露加密密钥的情况下执行加密和解密操作。这是最安全的方式之一，因为密钥被存储在一个专用的硬件或软件区域，当设备支持时。

- **如何使用**：
    - 生成并存储加密密钥在 Android Keystore。
    - 使用该密钥进行加密和解密，但不直接导出密钥。

```java
KeyGenerator keyGenerator = KeyGenerator.getInstance(
        KeyProperties.KEY_ALGORITHM_AES, "AndroidKeyStore");
keyGenerator.init(
        new KeyGenParameterSpec.Builder("keyAlias",
                KeyProperties.PURPOSE_ENCRYPT | KeyProperties.PURPOSE_DECRYPT)
                .setBlockModes(KeyProperties.BLOCK_MODE_CBC)
                .setEncryptionPaddings(KeyProperties.ENCRYPTION_PADDING_PKCS7)
                .build());
SecretKey key = keyGenerator.generateKey();
```bash

# KMS

# 动态生成aes密钥+rsa
每次随机生成aes密钥，然后采用预先定义的rsa公钥进行加密，到服务端后由服务端的rsa私钥进行解密拿到aes密钥，再对数据进行解密【开销比较大】
